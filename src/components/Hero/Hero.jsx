import { HeroContainer, HeroTitle } from './Hero.styled';

const Hero = () => {
  return (
    <HeroContainer>
      <HeroTitle>
       World's #1 Cars Rental 
      </HeroTitle>
    </HeroContainer>
  );
};

export default Hero;
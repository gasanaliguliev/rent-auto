import { HeroContainer, HeroTitle } from './Hero.styled';

const Hero = () => {
  return (
    <HeroContainer>
      <HeroTitle>
       World's #1 Reantal cars
      </HeroTitle>
    </HeroContainer>
  );
};

export default Hero;